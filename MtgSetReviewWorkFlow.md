# MTG 新系列评估工作流

> 用途：对一个新系列建立可复现的限制赛环境判断，完成全卡逐张评价，并系统发掘对现有构筑的补强牌与可能形成新构筑的种子。
>
> 本流程评价的是某个时间点的系列快照，不把预览期判断、纸面推演或早期小样本写成稳定结论。所有结论必须带目标赛制、平台、数据截止时间和置信度。

配套产出模板见 [MtgSetReviewTemplate.md](MtgSetReviewTemplate.md)。构筑候选进入完整牌表后，转交 [MtgDeckCacuWorkFlow.md](MtgDeckCacuWorkFlow.md) 的模式 B（牌表优化）或模式 C（主题种子补全）。

## 适用模式

| 模式 | 触发条件 | 目标 | 允许的结论强度 |
|---|---|---|---|
| P 预览追踪 | 官方牌表尚未完整 | 记录新增卡、机制和待验证假说 | 仅临时评级，不产出“全系列最佳” |
| F 全卡表初评 | 官方完整牌表和规则说明已发布 | 完成全卡覆盖、限制赛骨架和构筑测试队列 | 纸面结论，不能宣称实战胜率 |
| E 上线后校准 | 已有对局日志或可靠聚合数据 | 修正评级、原型牌表和选牌策略 | 结论必须同时给样本口径 |
| R 定期复盘 | 环境、勘误、禁牌或平台版本变化 | 输出相对上一版的增删改 | 只重开受影响结论，不静默覆盖历史 |

系列成熟度与评价置信度分开记录：

- `C0`：牌面、规则或系列范围仍不完整。
- `C1`：完整牌面和规则下的纸面推演。
- `C2`：有明确口径的对局或聚合数据支持。
- `C3`：经过多个时间窗口、环境变化或交叉来源复核。

置信度不是强度。`C1` 的高评价牌仍可能是高优先级候选，只是结论容易被新证据推翻。

---

## 阶段 0：任务建档

### 0a. 必填参数

1. 系列：正式名称、系列代码、产品类型、预计或实际发售日。
2. 快照：本次数据截止到具体日期和时间；预览期记录已公开张数与预计总数。
3. 范围：主系列、额外牌表、特殊来宾、指挥官产品、衍生物和仅数字版牌分别开关，不得混成一个牌池。
4. 限制赛：Draft / Sealed、BO1 / BO3、平台、补充包类型、牌池构成、调度和特殊规则。
5. 构筑赛：逐项列出目标赛制、平台和 BO 类型。合法性必须逐牌读取，不能从系列名称推断。
6. 现有构筑基线：指定要扫描的牌表目录、外部环境牌表快照和截止日期。
7. 评价目标：竞技优先 / 主题启发优先 / 两者并行；预算或稀有度资源是否影响建议。
8. 语言：英文牌名作为稳定键，中文名来自明确数据源；暂无官方中文名时保留英文并标记待补。

交互任务在开工前确认以上参数。自动或批处理未收到参数时使用以下默认值并写入运行清单：

- 限制赛同时评估 Draft 与 Sealed，分别标注 BO1 / BO3 差异。
- 平台优先 MTGA；产品不在 MTGA 时改为实体牌并标记平台变更。
- 构筑先评估该系列合法的 Standard 与 Pioneer，再扫描仓库现有牌表所属赛制；Modern 只保留具有独特效率、组合关系或备牌价值的测试候选。
- 评价目标为竞技可用性与主题启发并行，预算不设上限。

### 0b. 范围冻结

在运行清单中记录：

- `set_scope`：哪些产品和 bonus sheet 计入限制赛牌池，哪些只计入构筑扫描。
- `card_snapshot`：牌张总数、已解析数、未解析数、数据抓取时间和来源版本。
- `rules_snapshot`：综合规则、系列机制文章、Release Notes 或勘误的版本日期。
- `format_snapshot`：每个构筑赛制的合法性、禁限牌和平台可用性基准日。
- `deck_snapshot`：现有牌表与环境牌表的文件或来源版本。

预览期每次新增卡都以增量批次处理。旧牌发生勘误或牌面更新时，作为内容变更重新评价，不能只更新图片或收藏编号。

### 0c. 构筑工具包同步门禁

工具包是人工筛选后的主题相关牌集合，用于缩小后续构筑搜索范围；其中的牌不是默认必留，也不等同于最终候选池。每次系列或牌面规则更新后，必须先更新工具包：复核新增牌、移除失效牌、目标赛制合法性、平台印刷、角色标签和备注。完成同步后，构筑流程再从工具包中选取本次可能用上的部分，分别标记为主题种子、候选或备查；未被选中的工具包牌不得自动进入最终牌表。工具包版本、更新日期和对应系列快照写入运行清单。

---

## 阶段 1：数据获取与完整性

### 1a. 数据源优先级

1. 官方卡牌画廊、Release Notes、机制说明、产品和补充包构成：确定真实牌面、规则与限制赛可开出范围。
2. Scryfall 或等价结构化卡牌源：获取 oracle id、牌面、多面牌结构、费用、类别、颜色、稀有度、系列、收藏编号、合法性和平台字段。
3. mtgch / sbwsz：核对中文牌名与目标平台印刷；不得反向覆盖官方规则文本。
4. 平台客户端或官方牌表：复核数字版重平衡、可用性和导入名称。
5. 聚合对局数据、赛事结果和公开牌表：只在阶段 6 用于校准，不参与伪造初评的“客观分数”。

所有网络数据保存抓取时间、查询参数、分页完成状态、失败与重试项。HTTP 失败、查询语法错误、尚未公开和真实零结果必须是不同状态。

### 1b. 牌张身份和去重

- 以 `oracle_id` 或等价规则身份作为构筑评价主键，以系列代码 + 收藏编号保留该系列中的具体版本。
- 同规则文本的异画、闪卡和 showcase 只评价一次，但所有可开出版本仍参与补充包构成检查。
- 双面牌、历险、融合、原型等是“一张牌、多个可用模式”；每个面分别解析费用、类别、颜色、属性和规则文本。
- 数字版重平衡牌、Alchemy 版本或规则文本不同的变体必须分开评价。
- 基本地、衍生物、徽记和提示牌进入库存清单，但默认不计入“待评级张数”。特殊功能基本地按普通牌处理。
- 重印牌不能直接写“已知”。限制赛需要在新环境重评；构筑需要检查是否带来新的合法性、平台可用性、版本数量或组合关系。

### 1c. 完整性门禁 G1

进入全卡表初评前必须满足：

- 官方是否宣布完整牌表已经明确。
- 应评级数 = 已评级数 + 明确排除数，且未解释差值为 0。
- 所有多面牌均已读取 `card_faces[]` 或等价字段。
- 系列机制和关键词均有规则来源；未解决规则问题进入阻塞清单。
- 限制赛可开出范围与构筑合法范围已经分离。

未通过 G1 时可以继续模式 P，但所有汇总必须显示覆盖率，不能使用“全系列”措辞。

---

## 阶段 2：建立系列骨架

本阶段先描述环境需要解决的问题，不先挑“最强单卡”。产出限制赛基线和构筑机制索引。

### 2a. 机制与资源表

每个机制记录：

| 字段 | 内容 |
|---|---|
| 规则动作 | 实际发生的状态变化，含费用、时机、目标和次数限制 |
| 输入资源 | 手牌、坟场、衍生物、特定类别、法术力、生命等 |
| 输出资源 | 场面、卡差、节奏、伤害、法术力、选择权等 |
| 隐性成本 | 需要先有场面、额外卡位、公开信息、延迟回报、被响应窗口等 |
| 密度要求 | 独立牌、低密度协同、需要临界数量、只能专用套牌使用 |
| 规则风险 | 容易误读的替代效应、触发时点、目标、层或区域变化 |
| 跨赛制差异 | 限制赛正常使用方式与构筑中可能被放大的部分 |

任何“无限”“锁定”“必定触发”或“一卡达成”结论都必须写出完整状态、支付、优先权、循环净变化和中断点。

### 2b. 限制赛基线

按颜色和费用统计，并把金色、无色、地和多色费用单列：

- 生物曲线：各费用生物数量、常见力量 / 防御力、飞行 / 延势 / 警戒等关键异能。
- 身材断点：二至六费常见攻击力与防御力，哪些数值能有效进攻、交换或阻挡。
- 互动矩阵：费用、速度、目标范围、伤害 / 减值、是否放逐、是否亏牌、主要漏网目标。
- 战斗诡计：费用、公开程度、增幅、保护方式和被一换二风险。
- 卡差与滤牌：立即或延迟、需要先有场面还是落后可用。
- 法术力与调色：无条件色源、横置成本、宝物 / 生物调色、找地和三色回报。
- 终结与反僵持：飞行、践踏、不可阻挡、直伤、反复利用、磨牌和大型法术力出口。
- 主牌神器 / 结界 / 坟场依赖密度：决定窄解应主牌还是备牌。

在补充包构成未知时，只报告牌表数量，不把稀有度数量冒充实际 `as-fan`。只有取得真实 collation / slot 信息后才估算每包出现率，并保存公式与输入。

### 2c. 构筑机制索引

把每张牌的规则动作映射为标准标签，例如：

`产费`、`减费`、`额外施放`、`复制`、`牺牲出口`、`死亡回报`、`弃牌`、`自磨`、`回收`、`进场触发`、`离场触发`、`衍生物`、`类别回报`、`指示物`、`找牌`、`保护`、`封锁`、`替代效应`、`特殊胜利`。

标签只用于召回。最终结论必须复核对象、区域、时机、次数、费用和“may / if / instead”等条件，不能因共享关键词就宣布协同。

---

## 阶段 3：全卡逐张初评

### 3a. 两套评价坐标

限制赛和构筑赛必须分别评价，不使用一个总分混合。

限制赛采用锚定等级：

| 等级 | 定义 |
|---|---|
| A | 能显著改变胜负，通常值得早选且对套牌条件要求低；必须说明解答窗口 |
| B | 高质量牌或强协同核心，明显高于普通可用牌 |
| C+ | 高于平均的主牌，通常乐于使用 |
| C | 合格主牌，承担曲线或必要功能 |
| C- | 可用但容易被更好卡替换，或需要适度环境支持 |
| D | 窄用途、备牌或低质量补位，只在明确条件下使用 |
| F | 通常不应进入牌表；仍记录极端例外条件 |

Draft 与 Sealed 各给一个基础等级。强协同牌使用“基础等级 → 指定原型等级”，例如 `D → B（当坟场启用件不少于 X）`，不得把最理想套牌中的上限当成基础等级。`X` 必须来自牌表密度、实际套牌或数据，而不是固定套用。

构筑不使用 A-F，而使用两个字段：

- 用途：`现有补强` / `新增冗余` / `新轴启用` / `核心回报` / `备牌工具` / `法术力基建` / `观察` / `暂无明确用途`。
- 测试优先级：`T0 立即构筑验证`、`T1 定向测试`、`T2 等待环境或更多组件`、`T3 当前不投入测试`。

`T0` 不等于必然会成为主流，`T3` 也不等于永久不可用。每个结论都要带目标赛制和 `C0-C3` 置信度。

### 3b. 每张牌必答项

1. 身份：收藏编号、颜色、稀有度、费用、类别、各牌面与合法性。
2. 角色：威胁、互动、引擎、回报、调色、卡差、终结、备牌或组合组件；可多选但标主角色。
3. 费用真实性：牌面费用、额外费用、启动费用、维持费用与颜色强度分别记录。
4. 场面测试：领先、均势、落后三种状态下做什么，是否立即影响场面。
5. 下限 / 常态 / 上限：各自需要哪些可验证条件，不能只描述理想情况。
6. 可替代性：同费用同角色的基线牌是什么，新牌改善了什么、牺牲了什么。
7. 脆弱点：常见互动、时机、被一换二、传奇冗余、自身冲突与资源耗尽。
8. 限制赛：Draft、Sealed、适用原型、主牌 / 备牌、先手 / 后手或 BO 差异。
9. 构筑赛：逐目标赛制写用途、测试优先级、适配套牌或新轴假说。
10. 证伪条件：什么观察会让当前评级上调或下调。

### 3c. 限制赛检查表

- 生物不只看费用身材，还要检查能否攻击、能否有效阻挡、进场价值、死亡后价值和被常见互动处理的节奏损失。
- 去除不只看“能杀”，还要检查合法目标覆盖率、速度、颜色强度、附加条件和对炸弹的答案率。
- 战斗诡计同时评价成功收益与被响应后的损失；开放法术力本身也是机会成本。
- 抓牌和引擎必须说明开始回本的回合、场面成本和落后时表现。
- 协同牌必须分清 enabler 与 payoff，并估算一副实际牌表需要的最低密度。
- 高费牌必须证明环境速度、调色和生存能力允许施放；稀有度不能替代这一证明。
- 窄针对牌根据主牌目标密度决定 `C- / D / F`，并单独给 BO3 备牌价值。
- 牌序评价使用档位和比较组，不伪造跨颜色的绝对第 1 至第 N 名精确顺序。

### 3d. 全卡覆盖门禁 G2

- 所有应评级牌都有 Draft、Sealed、构筑用途、测试优先级和置信度。
- 所有 A、B、T0、T1 结论有至少一句可证伪理由，不允许只有结论。
- 所有 D / F 牌记录可能使其可用的条件，避免永久遗漏窄牌。
- 重印牌已在新限制赛环境重评；多面牌已按全部模式评价。
- 汇总中的数量能回算到逐卡表，未评级数为 0。

---

## 阶段 4：限制赛环境合成

逐卡初评完成后再做环境合成，并允许上下调整评级。调整必须进入变更日志。

### 4a. 原型矩阵

至少覆盖十个双色组合，并根据实际牌池增加单色、三色、五色或非典型原型：

| 字段 | 要求 |
|---|---|
| 主计划 | 如何建立优势并实际结束游戏 |
| 次计划 | 核心协同不足时能否按普通曲线运作 |
| 启用件 | 低稀有度可获得数量、费用和是否被其他原型争抢 |
| 回报 | 回报强度、需要的临界密度和抽取顺序风险 |
| 曲线骨架 | 二至五费动作、互动、终结和建议地数；不是固定模板 |
| 关键普通 / 非普通 | 支撑原型而非只展示主题的牌 |
| 调色与溅色 | 色源数量、时间成本、值得溅色的牌和失败代价 |
| 弱点 | 速度、飞行、防大生物、资源战或组件被拆后的表现 |
| 结论 | 强 / 可行 / 依赖开放度 / 暂不成立，并带置信度 |

金色非普通只是设计信号，不是原型成立的证据。原型成立至少需要低稀有度启用件、足够回报、合理曲线和不依赖特定抽取顺序的次计划。

### 4b. 环境级问题

1. 速度：关键抢血回合、首个有效阻挡回合、先后手差异和快攻惩罚慢牌的方式。
2. 互动：主要去除能覆盖哪些身材 / 类别，炸弹是否存在可获得的答案。
3. 资源：抓牌、法术力沉没、坟场利用和僵持局出口是否普遍。
4. 调色：双色是否自然、三色是否有真实代价、抓地与调色是否被所有人争抢。
5. 协同密度：按原型统计启用件与回报，不把同一张被多个原型争抢的牌重复视为稳定供给。
6. 轮抽信号：哪些高质量单色牌保持开放，哪些金色 / 窄协同牌只在方向明确后提高顺位。
7. Sealed 差异：炸弹、去除、调色、牌池深度和比赛节奏如何改变评价。

### 4c. 限制赛交付

- 环境摘要：预计速度、互动质量、调色、主要身材断点、炸弹与反僵持手段。
- 原型梯队：强度与稳定性分开，不使用一个榜单掩盖高上限低稳定原型。
- 按颜色的普通 / 非普通优先组和可替换组。
- 去除、战斗诡计、调色、主牌窄解与备牌表。
- Draft 与 Sealed 的差异化建议。
- 仍待对局验证的关键假说列表。

---

## 阶段 5：构筑可用性挖掘

每个目标赛制独立执行。Standard 的结论不得直接复制到 Pioneer / Modern，Brawl 的颜色身份和指挥区可访问性也不得套用普通构筑模型。

### 5a. 现有思路补强

1. 建立基线：读取仓库牌表和基准日环境牌表，提取主轴、各功能卡位、曲线、色源、弱对局和当前同角色基准牌。
2. 做角色匹配：新牌必须和具体卡位比较，不因同一关键词就判定为升级。
3. 检查增益类型：效率升级、第五至第八张冗余、主牌覆盖面、备牌工具、色源改善、减少组件数或提高可检索性。
4. 检查代价：颜色、速度、传奇、卡位、牌型密度、导师限制、自冲突、平台可用性和换备后结构变化。
5. 生成最小改动提案：明确 `换入 / 换出 / 数量 / 理由 / 风险 / 成功指标`，再交给组牌优化工作流做完整合法性和概率校验。

补强表必须包含：

`现有牌表 | 当前卡位 / 基准牌 | 新牌 | 改善 | 退步 | 建议数量 | 测试对局 | 结论`

### 5b. 新思路发现

从规则动作而不是宣传机制名称出发：

1. 为 T0 / T1 的独特牌写出 `输入资源 → 转换动作 → 输出资源 → 制胜方式`。
2. 在目标牌池检索同类启用件、回报、导师、冗余件、保护和备用赢点；同一概念至少使用两组规则文本查询。
3. 区分核心、冗余、加速、保护、互动和终结，计算最小组件包占用的卡位与颜色。
4. 写出最早可执行回合和真实支付顺序；跨阶段法术力、目标、优先权、区域变化、传奇规则和“一回合一次”逐项核对。
5. 对手拥有常见互动时重放流程，记录在哪个点失败以及能否恢复。
6. 与已有成熟策略比较：新轴必须至少在速度、韧性、卡位、可检索性、交互能力或独特对局之一有可测试优势。
7. 产出 60 张 / 100 张完整牌表前，先交付 8 至 20 张的“最小原型包”和两个候选外壳，避免用填充牌伪装完成构筑。

新轴假说格式：

`种子牌 | 目标赛制 | 核心循环 / 价值链 | 最小组件 | 外壳 A / B | 互动弱点 | 备用赢点 | 证伪条件 | 优先级`

### 5c. 构筑门禁 G3

- 每张牌在每个目标赛制都有用途和测试优先级；非法或平台不可用单独标记，不能混作低强度。
- T0 / T1 牌均已对照同费用同角色基线，而不是只写牌面优点。
- 所有“现有补强”指向具体牌表与卡位，给出最小变更和测试指标。
- 所有“新轴启用”写出最小组件、实际赢法、交互点和备用计划。
- 组合技通过完整规则与状态变化核验；无法确认时标记规则待验证，不发布确定性结论。
- 构筑牌表若已生成，必须按目标赛制重新执行数量、同名上限、禁牌、颜色身份、平台和色源门禁。

---

## 阶段 6：实测与数据校准

### 6a. 限制赛日志

每次 Draft / Sealed 至少记录：

- 日期、平台、队列、BO、段位或赛事层级、是否有手牌平滑等平台规则。
- Draft 的选牌记录、座位 / 颜色转向点、最终未采用牌；Sealed 的完整牌池与备选构筑。
- 最终牌表、地数、曲线、原型、启用件 / 回报数量和溅色来源。
- 对局先后手、对手原型、调度、关键牌是否施放、表现与失败原因。
- 规则误判、被高估的理想情形和未预见的互动。

个人战绩只能支持“本次样本观察”。没有足够样本和对照时，不能把 `3-0` 或 `0-3` 直接转成环境结论。

### 6b. 聚合数据口径

使用公开聚合数据时同时保存：数据窗口、平台、队列、段位、样本量和指标定义。重点检查：

- 选取顺位只代表供需与玩家认识，不直接等于强度。
- 手牌胜率、抓到后提升、主牌胜率等指标受牌手水平、颜色组合、套牌质量和选择偏差影响。
- 协同牌先看对应原型与满足密度的子样本，再看全局平均。
- 小样本牌、稀有牌和刚上线窗口必须显示不确定性，不按小数点后差异排序。
- Draft 数据不能直接校准 Sealed，BO1 不能直接校准 BO3。

每次数据校准必须保留“原评级 → 新评级、证据、样本口径、解释”，禁止静默改分。

### 6c. 构筑测试

对每个 T0 / T1 候选建立测试卡：

| 字段 | 内容 |
|---|---|
| 假说 | 新牌改善的具体问题 |
| 对照 | 被替换牌或不使用新牌的基准版本 |
| 版本 | 主 / 备数量和所有连带修改 |
| 对局 | 预期改善、持平和恶化的代表 matchup |
| 观察 | 按时施放率、死牌率、实际换牌质量、卡位冲突、色源压力 |
| 成功条件 | 进入下一轮的明确条件 |
| 失败条件 | 回退、减量或更换外壳的明确条件 |

探索性少量对局先验证机制是否按预期运作，再扩大样本比较对局表现。比赛数和小局数分别记录；不同 matchup、先后手、BO 和牌表版本不得混成一个胜率。

---

## 阶段 7：交付与复盘

### 7a. 建议目录

每个系列使用独立目录：

```text
SetReview/{SET}_{YYYYMMDD}/
  00_RunManifest.md
  01_SetOverview.md
  02_LimitedEnvironment.md
  03_CardRatings.md
  04_ConstructedWatchlist.md
  05_TestLog.md
  06_ChangeLog.md
```

小型预览批次可以先使用一份报告；进入模式 F 时再拆分。原始 API 响应与派生表应放在同一运行目录的 `data/` 下，并记录来源与生成方式。

### 7b. 最终报告顺序

1. 运行清单与覆盖率。
2. 一页结论：限制赛环境、最稳定原型、最大风险、构筑 T0 / T1 候选。
3. 机制和规则风险。
4. 限制赛基线、原型矩阵和 Draft / Sealed 建议。
5. 全卡逐张评价。
6. 现有构筑补强表。
7. 新构筑种子与最小原型包。
8. 测试计划、尚未解决的问题和证伪条件。
9. 相对上一快照的变更日志。

### 7c. 最终门禁 G4

- 覆盖率、数据时间、目标赛制、平台与系列范围在报告首屏可见。
- 预览、纸面推演、个人实测和聚合数据结论使用不同标签。
- 限制赛评级与构筑测试优先级没有混成一个“总分”。
- Draft 与 Sealed、BO1 与 BO3 的显著差异已分开。
- 每个高优先级结论都有理由、风险和证伪条件。
- 所有统计可回溯到输入与口径；失败请求和缺失数据没有被吞掉。
- 所有构筑建议通过对应赛制和平台合法性检查。
- 相对上一版的评级、候选和假说变更有 diff。

---

## 更新节奏

建议在以下事件触发增量复盘，而不是每次从头重写：

1. 官方预览批次：只评价新增 / 变更牌，并更新受影响机制与原型假说。
2. 完整牌表与 Release Notes：执行 G1、全卡初评和首次完整环境合成。
3. 上线后早期窗口：优先验证规则、环境速度、互动断点和 T0 原型，不把早期热度当稳定环境。
4. 第一个完整数据窗口：按队列和原型校准逐卡评级，输出第一次数据 diff。
5. 重要赛事、补丁、勘误或禁牌后：只重开被新环境条件影响的评价，并提高或降低置信度。

每次更新必须保留上一快照。结论被推翻是正常结果，缺少变更理由才是流程失败。

---

## 执行分工与审计

工作量较大时可按以下责任拆分，但最终必须由同一运行清单合并：

- 数据 / 规则审查：负责完整性、牌面、多面结构、规则问题、合法性和来源。
- 限制赛审查：负责身材断点、互动、原型、逐卡 Draft / Sealed 评级。
- 构筑审查：负责现有牌表映射、跨牌池检索、新轴原型与测试计划。
- 反向审计：专门寻找高评级牌的失败情形、组合规则错误、样本偏差和遗漏牌。

审查意见冲突时不强制平均评级。保留各自理由，列出能区分两种判断的测试，并在新证据出现后裁决。
