> 用途：给定一个套牌主题（如"纯绿种地"），按固定流程完成环境调研、候选牌枚举、构筑、验证与交付。
默认参数：娱乐向但保证强度、MTGA 牌池、BO3、无预算上限。如当次需求不同，在阶段 1 覆盖。自动评测或批处理允许使用显式记录的默认值继续执行，不应因人工确认点阻塞；交互构筑仍须等待用户确认。

适用模式（开工时先判定）：

- A. 从零构筑：给定主题，走全流程（阶段 0–5）。
- B. 牌表优化：输入既有牌表，先执行阶段 0b 体检，再走阶段 1–5；阶段 2 的枚举以"同角色更优候选"为目标，阶段 5 交付必须含"改动对照 diff"（见阶段 5）。
- C. 主题种子补全：输入未达到目标赛制构筑要求的主题锚点 / 候选集合，先在阶段 0b 区分“有意种子”与“损坏导入”，记录必留与可调锚点，再按阶段 1–5 补成目标赛制牌数；阶段 5 交付“种子保留 / 增量补全”而非伪造逐张砍牌 diff。

---

阶段 0：主题澄清（开工前必须完成）

与用户确认以下参数，写入任务备忘：

1. 主题边界：主题的核心机制是什么？排除什么？（例：本次为"与地相关 Lands Matter"——放地进场、按地数量缩放、地变生物；明确排除纯地落 Landfall，但边缘牌列为候选由用户定夺）
2. 代表牌：让用户举 2–3 张心目中符合主题的牌，作为主题判定的锚点（例：荒野之魂艾莎娅、茁壮花身）
3. 定位：娱乐向 / 竞技向；预算帽；牌池（MTGA / 实体 / MTGO）；目标赛制与队列（如 Pioneer、普通 Brawl、Competitive Brawl）；对局类型（BO1 / BO3）；颜色约束（纯绿 / 允许混色）
4. 边缘牌策略：不严格符合主题但顺带契合的牌 → 列入候选并打标记（本文用 ◇），由用户最终取舍
5. 行侣策略：禁用 / 作为候选 / 强制指定。若指定行侣，记录其构筑条件、主牌最小张数、占用的备牌位置和当前规则下从备牌置入手牌的费用 / 时机。
6. 指挥官策略：仅在 Commander-style 赛制启用。记录指挥官候选、颜色身份、首次施放费用、指挥官税、可从指挥区稳定访问的收益及该赛制是否允许鹏洛客 / 载具等其他类型担任指挥官。

---

阶段 0b：牌表体检（模式 B / C）

1. 输入语义分类（必须先于合法性结论）：区分完整牌表、主题种子 / 锚点集合、候选池与损坏导入。少于 60 张只能说明它不能直接参赛，不能单凭数量判定为损坏；结合文件说明、是否缺地 / 备牌、牌名集合结构与用户措辞决定走 B 还是 C。模式 C 记录每张种子为必留 / 可调 / 仅供参考。
2. 玩法推断（必须先于一切改动，并与用户确认）：从牌表推断套牌的核心思路 / 主轴运作（制胜路径）/ 附带轴线，用一句话概括玩法。后续所有优化必须围绕已确认的主轴，不得漂移到相邻主题（实测教训：这套纯绿的主轴是"铺地→地数量缩放生物身材→beatdown 取胜"，艾莎娅/培护巨像/豆茎巨人是身材缩放回报；误判为 landfall 主题会导致引擎选择跑偏——眼镜蛇/比利/梓纱服务的是地落触发。标注角色时必须区分"地落触发"与"地数量缩放"这两类牌）。
3. 读取目标赛制构筑档案后解析牌表并逐张加总，禁止把 `60/15` 硬编码到所有赛制。普通构筑赛档案默认为主牌不少于 60、备牌 0–15，优化交付默认收敛到 60/15；未填满备牌应标记为"合法但卡位未利用"。MTGA Brawl 档案为 1 名指挥官 + 99 张牌、除基本地和牌面例外外单卡、颜色身份匹配、无备牌；普通 Brawl 与 Competitive Brawl 的禁牌、调度和匹配规则分别记录。模式 C 应明确标为“种子尚不可参赛，等待补全”，不把它与非法单卡或损坏格式混为一谈。
4. 每张牌的三重核对前置执行：目标赛制合法 / 目标平台可用 / 中文名（方法同阶段 4）。赛制和平台必须读取阶段 0 参数，不能硬编码为先驱或 MTGA；实体牌无需 Arena 可用性，MTGO 与 MTGA 分别核对。任何硬门禁失败的牌直接标红。
5. 给每张牌标注功能角色（核心回报 / 引擎 / 终结 / 互动 / 地），产出曲线与地当量，找出结构缺口（缺引擎、缺卡差、回报错位等），作为后续优化的靶子。多模式牌（MDFC / 历险 / 伪装 / 预备等）必须逐面标注：每个面单独记录费用与角色，并考虑各模式的时间灵活性——如预备机制可先进场站场、后续任意回合再施放副本（实测教训：Studious First-Year 可 1 费进场、Rampant Growth 副本留到 5 费再放）；费用一律以 API 实测为准，不得凭记忆（实测教训：杂生花晶石实为 {1}{G}，曾被误记为 1 费）。

---

阶段 1：环境基线确认

1. 赛制时间范围：确认当前日期，通过 Scryfall `/sets` 列出已发售系列，并按目标赛制定牌池边界（例如先驱从 RTR 起；Modern 从第八版起且包含直入 Modern 系列）。注意：`/sets` 会列出未发售系列，格式查询也可能提前包含未发售牌——牌池以"已发售"为准，所有检索统一加入 `date<={基准日期}`；仅在 API 不支持日期过滤时才用 `-e:{系列码}` 逐个排除。保存本次系列基线与查询日期。
2. 禁牌表：Scryfall 查询 `banned:{format}`（如 `banned:modern`）+ 网络检索最近一次禁牌公告交叉验证。必须区分赛制基础禁牌与平台/队列特例（如仅 MTGA BO1 禁用），记录公告生效日期。普通 Brawl、Standard Brawl 与 Competitive Brawl 是不同合法性 / 禁牌字段和队列，不得互相复用结论。历史上曾被禁但当前已解禁的牌必须以本次基准日状态为准，不能沿用模型记忆。
3. 环境粗扫：搜索官方公告 / 环境文章，确认主题思路在当前环境的强度定位（娱乐 / 可行 / 主流），只需大方向。

---

阶段 2：候选牌枚举（核心调研）

2a. 需求分解 → 模块

把套牌拆成功能模块，每个模块一组检索。通用模块模板：

模块	内容	本次实例	
M1 核心回报	主题机制的 payoff 牌	地数量缩放生物	
M2 引擎	让主题机制转起来的牌	放地进场、额外下地	
M3 终结 / 放大器	终结手段与资源放大	大法术力、大生物	
M4 特殊卡位	不占主结构的功能牌	功能地、双面地 MDFC	
M5 互动	去除、保护、反制康	互斗、点杀、辟邪	
M6 备牌	目标赛制允许备牌时，基于主牌弱点的换备方向	坟场针对、控制针对
M7 行侣	合法行侣、构筑约束与确定可访问资源	约力昂 80 张、备牌容量与闪烁回报
M8 生存 / 制胜	主牌苟命预算与独立赢点	阻挡 / 回血 / 去除 / 扫场；token / 吸血 / 战斗终结
M9 指挥官	Commander-style 赛制的合法指挥官、颜色身份与指挥区收益	Brawl 指挥官、指挥官税、确定可访问引擎

2b. 逐模块 + 逐系列遍历

- 工具：Scryfall API（`/cards/search`），统一过滤条件：
  - 牌池：`f:{format} date<={基准日期}`，仅当目标平台为 MTGA 时追加 `game:arena`；实体/MTGO Modern 示例为 `f:modern date<=2026-08-01`。Brawl 必须使用与目标队列对应的合法性字段，并以 Arena 官方禁牌表复核，不能拿 Historic 或纸质 Commander 代替。
  - 颜色约束翻译：阶段 0 的颜色约束直接转为过滤器（纯绿且允许无色 = `ci<=g`；必须恰为绿色身份才用 `ci:g`；允许白绿 = `ci<=wg` 之类），不要让枚举结果混入违规色牌再靠人工剔除
  - 结果按 `oracle_id` 去重，保留有 Arena 版本的印刷
- 检索方式：按异能关键词构造 oracle 文本查询（如 `o:"number of lands you control"`、`o:"put a land card" o:"battlefield"`），覆盖赛制内全部系列后按系列分组呈现，等价于逐系列遍历。每个模块至少用 2 种措辞检索（短语查询会假性 404，见坑位清单），避免漏牌。
- 补充检索：对记忆中有印象但未被关键词命中的牌，逐张 `/cards/named` 复核合法性与平台可用性（本次教训：Explore 不合法、Rampant Growth 不合法、Dictate of Karametra 不在 Arena——记忆必须验证）。无关键字但主题相关的牌（如 Tatyova、Multani 之于种地）只能靠这一步兜底。

执行保障：

- 遵守 Scryfall 当前速率限制；批量精确查牌优先使用 `/cards/collection`，搜索请求之间节流，并对 `429/5xx` 使用指数退避与 `Retry-After`。
- 按规范化查询参数缓存原始响应，记录抓取时间、数据源、HTTP 状态与失败项；支持断点续跑，不得以部分成功结果宣称"全部通过"。
- 将 HTTP / 查询语法失败、分页不完整、模糊名未精确命中与真实零结果分开建模；任何请求失败都不得自动转换成“赛制不合法”或“平台不可用”。只有完整遍历目标 oracle 的全部印刷后才能下平台结论。
- 所有搜索处理分页；同一次运行复用 oracle 级结果，平台可用性仍须检查任一印刷，不能只看 `/cards/named` 返回的最新印刷。
- "逐系列遍历"的统计口径必须写入运行记录：候选以 `oracle_id` 去重，系列命中按原始印刷分别统计，不能把去重后保留的单一印刷误当成系列覆盖率。
- 大牌池的宽查询只用于召回，不直接作为候选清单。记录每个模块的原始命中数后，按精确规则模板、费用、颜色、卡牌类型、可检索性与自冲突做第二阶段过滤；命中过多时输出可解释的截断/排序规则。

2c. 双源二次核对（Scryfall + sbwsz.com / mtgch.com API）

每张候选牌执行：

1. 中文名：`GET https://mtgch.com/api/v1/card-names/?q={英文名}` → 响应字段为 `items[]`，取 `translated_name`（注意：不是 `results`/`data`）
2. 赛制合法性：读取 Scryfall oracle 级 `legalities.{format}`，并与基准日官方禁牌表交叉验证
3. 平台可用性：MTGA 使用 `GET /api/v1/result?q={牌名}&view=0&unique=scryfall_id`（分页），任一版本 `arena_id` 非空即通过；实体牌确认存在已发行纸质印刷；MTGO 使用 Scryfall `games`/对应平台数据，不得拿 `arena_id` 代替
4. 判定口径：赛制合法与平台可用是两个独立字段。新印版本可能不在目标平台，但旧版可用时 oracle 牌仍可通过；运行记录必须保存实际通过的平台印刷

已知坑位清单：
- PIO（先驱大师赛）2024-12 已登陆 Arena，其独有牌可用（例：云游者梓纱在先驱合法且 Arena 可用）
- Scryfall `/cards/named` 返回最新印刷，`games` 字段可能误导（需查全部印刷）
- MDFC / 历险 / 双面牌：Scryfall 顶层 `oracle_text`、`mana_cost`、`power/toughness` 为空，正背面完整文本在 `card_faces[]` 里——核对牌面异能时必须读 `card_faces`，否则会"看不到第二面"（实测踩坑：Beanstalk Giant // Fertile Footsteps、Studious First-Year // Rampant Growth 顶层文本为空）
- `o:` 短语查询可能假性 404：实测 `o:"whenever a land enters"` 零结果，换 `o:"landfall"` 命中 43 张；同一概念务必准备多种措辞
- 脚本直连 Scryfall 必须带 User-Agent（Python urllib 无 UA 会 400；curl 默认 UA 正常）
- Windows 环境：Python 默认 GBK——`json.load` 需 `encoding='utf-8'`，控制台输出需 `PYTHONIOENCODING=utf-8`，否则中文与破折号乱码/报错
- SBWSZ `/result` 的 `!""` 精确语法不可用；`page_size` 过大需分页；含撇号牌名用片段模糊查
- 大造物者卡恩为先驱禁牌但当前 Modern 合法；任何跨赛制建议都必须重新读取 `legalities.{format}`，不能把某一赛制结论复用到另一赛制
- subtype 必须保留所属 card type 命名空间：Shrine 是 enchantment subtype，即使出现在结界生物的类型栏也不是 creature subtype，不能为“选择一个生物类别”的效应选择 Shrine。部族地 / 类别回报候选必须校验效应要求的 subtype 类型，不能只做字符串相等。
- Brawl / Alchemy 重平衡牌：原 oracle、`A-` 版本和同角色替代牌分别验证。记忆中曾在 Arena 出现不代表基准日仍有可导入版本；替换时记录原候选、实际通过的 Arena 印刷和异能差异。

2d. 候选清单产出格式

按模块列表，每张牌一行：中文名 | English | 系列代码 | 一句定位，边缘牌打 ◇ 标记。附"系列遍历总表"（各系列命中数）。

---

阶段 3：构筑

1. 多方向提议：基于候选池给出 3–5 个构筑方向（思路 / 主轴 / 风格对比表），用户选定 1–2 个（可组合）。模式 B 则改为：针对体检发现的结构缺口给出 2–3 个优化方案（激进换血 / 微调 / 混合）。
   - 每个方向必须声明优化目标及优先级：赛制胜率 / 主题保真 / 预算 / 操作复杂度 / 牌表改动量。自动评测时使用预设权重并写入运行记录，避免把主观选择伪装成唯一最优解。
2. 初稿：按目标赛制构筑档案确定分区和牌数。普通构筑默认主牌 60 + 备牌 15；若采用行侣则按其条件调整主牌，并把行侣计入备牌上限（约力昂为 80 主牌 + 1 行侣 + 14 个普通备牌位置）。MTGA Brawl 使用 1 名指挥官 + 99 张牌且无备牌，曲线、抽牌概率和地牌比例只以 99 张牌库为母体；指挥官另记为确定可访问资源。
   - 逐牌附入选理由；列出"落选候选"及取舍逻辑，便于迭代
   - 主牌必须建立生存预算：按关键回合分别统计可用的阻挡、回血、进场横置 / 税收、点 / 放逐去除、扫场和重建资源，并以真实色源与回合费用校验。备牌不能替代首局的生存能力。
   - 主牌必须给出至少两个独立赢点，说明从稳定场面到致胜的实际转换动作；分别检查它们是否被自身扫场、传奇规则、颜色源、平台限制或主轴依赖抵消。主题资源递增本身不是赢点。
   - 砍牌/替代理由必须通过可验证门禁：引用的费用与异能须与牌面某一面的原文一致（实测教训：把 Flourishing Bloom-Kin 的伪装费用 {4}{G} 当成全牌费用，误判"5费太慢"）；"上位替代"仅限同费用段、同角色之间的比较
   - 引擎-资源匹配校验：铺地/加速引擎分"牌库找地"与"手牌下地"两类，后者仅当套牌有充足抓牌/手牌地补给时才有效（实测教训：梓纱/依吕夏树灵在零抓牌套牌中异能空转，人工研审判定提升有限）
   - 地源统计必须拆开：`地牌卡位`（真地 + 可当地使用的 MDFC）、`加权地当量`（默认真地 + MDFC×0.75，仅作启发式）、`起手可下地源`、`未横置绿源`、`牌库找地`、`手牌额外下地`。找地牌不得直接加进地当量；Bushwhack 一类"找地到手"是条件地源而非加速。另校验基础地目标数量，避免找地引擎在中盘失效。
   - 多色与特殊地必须建立“逐咒语色源矩阵”：把色源分为无条件、牌型限制、传奇限制、依赖场上永久物颜色、滤色和只能支付异能等类别，逐张判断关键咒语可用源。`White Lotus Hideout` 一类只能施放 Shrine / Lesson 的地不得记为普通五色源，`Plaza of Heroes` 的传奇施放模式也不得用于普通支援牌。
   - 找地 / 导师目标不仅检查初始数量，还要估算连续触发后的目标消耗、颜色分布与牌库剩余状态；一次找 X 张和每次触发找 1 张的引擎分别建模。
   - 对起手地源和关键回合颜色需求做超几何概率或蒙特卡洛校验，输出留牌阈值；不得只凭总地数判断法术力稳定性。
   - 行侣版本必须与非行侣最小牌数版本并排计算：地牌比例、起手关键色、四张核心在前 N 张的可见率、候选命中密度和调度阈值全部使用实际 starting deck 张数。行侣在牌库外，不进入超几何母体；另记为每局确定可访问但需支付当前行侣规则费用的资源。
   - 传奇牌重复数分开评估：自然抽取冗余、可共存上限、传奇规则前已经产生的 ETB 价值、弃牌 / 坟场价值。不得把“传奇”直接翻译成“牌表只能一张”。
   - 地牌的直接生命成本单独汇总：痛地 / 五色地的有色启用次数与预计失血必须作为色源方案代价输出，不能被“总色源充足”掩盖。
   - 给曲线、经典回合节奏、核心配合说明；回合节奏必须通过费用校验：无加速时第 N 回合只能打出法术力值 ≤ N 的牌，有加速时须写明加速来源与条件（实测教训：误写"1回合眼镜蛇/幼兽"——两者均为 {1}{G} 的2费生物，最早第2回合登场）
   - 对跨步骤 / 阶段支付建立 trigger budget：记录主阶段咒语、结束步骤追加费用、复制异能费用及必须保持未横置的来源。法术力默认在步骤 / 阶段结束时清空，除非牌面明确允许保留；不能用第一主阶段的临时 mana 自动支付结束步骤触发。
   - Commander-style 赛制另建指挥官可访问性模型：分别验证首次施放、每次增加 `{2}` 的指挥官税、指挥官被去除后的重建节奏和起动式异能色源；不得把指挥官混入牌库自然抽取概率。
3. 数字校验：按目标赛制逐区逐张加总复核（普通构筑检查主 / 备，Brawl 检查 Commander / Deck；本次教训：初稿曾 61 张）

---

阶段 4：验证与迭代

1. 逐牌三重核对打勾：目标赛制合法 ✓ / 目标平台可用 ✓ / mtgch 中文名 ✓
2. 典型对局推演：对快攻 / 对中速 / 对控制的起手、节奏、换备思路
3. 按用户反馈从"可调仓位"迭代，直至锁定
4. 机器门禁：先加载目标赛制构筑档案，再校验各分区数量、同名上限与基本地 / 牌面例外、目标队列禁限牌、颜色身份、发售日期、目标平台任一可用印刷和导入格式。普通构筑档案检查主牌不少于 60、备牌不多于 15；Brawl 档案检查指挥官恰为 1、牌库恰为 99、无备牌、单卡规则、指挥官类型合法及牌库颜色身份为指挥官颜色身份子集。Arena 任一印刷只在平台为 MTGA 时检查。另校验受限色源不得计作关键咒语的无条件色源，以及“选择生物类别”等效应只能使用对应 card type 的 subtype。声明行侣时还须校验：至多一个、行侣本身合法、计入备牌 15 张上限、starting deck 满足其条件、所有换备方案后仍满足条件，且抽牌概率不含行侣。优化器默认让换入数等于换出数以维持目标主牌张数；若不等量必须在方案中显式标注并重新执行行侣条件。
5. 将定性推演与实测分开标注。没有对局日志或模拟样本时只能写"推演"，不得输出未经数据支持的胜率结论。

---

阶段 5：交付

牌表格式（MTGO / MTGA 导入兼容）

- 每行 `数量 英文名`，按英文名字母升序混排（不分地/非地）
- 主牌与备牌之间空一行
- MTGA 导入时在备牌块前加 `Sideboard` 行
- 声明行侣时使用 `Companion`、`Deck`、`Sideboard` 三块；`Companion` 中的牌计入备牌总数但不在 `Sideboard` 块重复列出
- MTGA Brawl 使用 `Commander`、`Deck` 两块，不生成 `Sideboard`；指挥官只在 `Commander` 中出现一次

示例：

```
4 Arboreal Grazer
3 Ashaya, Soul of the Wild
...

1 Bala Ged Recovery
2 Carnage Tyrant
...
```

完整交付文档包含

- 最终牌表（上述格式 + 分功能表格版：`数量 | 费用 | 中文名 | English | 定位`，按费用（法术力值）升序排列，地牌列最后；费用字段一律以 Scryfall API 实测为准，不得凭记忆填写）
- 改动对照 diff（模式 B 必含）：砍 / 加 / 数量调整逐张列出，各附一句理由；同时列出"考虑过但排除"的候选及原因（含违反颜色约束等硬性排除项）。模式 C 改为列出种子原样保留、种子数量调整、增量补全和考虑过但排除，不伪造不存在的砍牌记录。
- 留牌指引、打法要点、核心配合、对局注意事项
- 备牌换入换出简表（仅目标赛制允许备牌时）
- 可调仓位清单（后续自行微调的备选池）
- 运行清单：基准日期、格式/平台/BO 类型、参数假设、数据源抓取时间、查询式、失败/重试项、候选与最终牌表校验摘要，保证结果可复现

---

附：常用 Scryfall 查询模式（种地主题示例）

```
f:pioneer game:arena date<=2026-08-01 ci<=g o:"number of lands you control"      # 地数量缩放
f:pioneer game:arena date<=2026-08-01 ci<=g o:"put a land card" o:"battlefield" # 放地进场
f:pioneer game:arena date<=2026-08-01 ci<=g o:"additional land"                  # 额外下地
f:pioneer game:arena date<=2026-08-01 ci<=g t:land o:"becomes a" o:"creature"    # 生物地
f:pioneer game:arena date<=2026-08-01 ci<=g layout:modal_dfc                     # 双面地
banned:pioneer date<=2026-08-01                          # 赛制基础禁牌表（平台/队列特例另查官方列表）
```

SBWSZ API 端点备忘：`/api/v1/card-names/`（中英名，响应字段 `items[]`）、`/api/v1/result`（搜索）、`/api/v1/card/{set}/{num}/`（单卡详情）、`/api/v1/sets/`（系列列表）、`/api/v1/docs`（完整文档）
